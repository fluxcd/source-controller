apiVersion: source.werf.io/v1
kind: HelmChart
metadata:
  name: podinfo
spec:
  interval: 1m
  sourceRef:
    kind: HelmRepository
    name: podinfo
  chart: podinfo
  valuesFiles:
    - values-prod.yaml
