apiVersion: source.werf.io/v1
kind: HelmChart
metadata:
  name: podinfo-git
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: podinfo
  chart: charts/podinfo
  valuesFiles:
    - charts/podinfo/values-prod.yaml
